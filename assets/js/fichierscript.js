$(function () { // équivaut à $(document).ready(function() {});
$("#text").show(); // show fait apparaitre l'élément (fonction native)
// $("#text").css("display", "block");// le changement de class css fait apparaitre l'élément
// $("#text").removeAttr("hidden"); // suppression de l'attribut HTML hidden fait apparaitre l'élément (fonction native)
// $("#text").toggle();//  permet d'afficher ou masquer l'élément (fonction native)
});